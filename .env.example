# Frontend (Expo) environment variables
# Copy this file to `.env` and adjust values as needed.
# Expo will inject variables prefixed with EXPO_PUBLIC_ into the app at build/runtime.

# Base URL of your backend API (no trailing slash)
# Example for local dev on same machine:
EXPO_PUBLIC_API_URL=http://<YOUR-LAN-IP>:5000
PORT=5000
HOST=0.0.0.0
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<name>?retryWrites=true&w=majority
JWT_SECRET=replace_with_strong_secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
